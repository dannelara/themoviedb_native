import React from "react";

import { View, Text } from "react-native";

export const Search = ({}) => {
  return (
    <View className="h-full items-center justify-center bg-dark_light">
      <Text className="text-red-500">Search</Text>
    </View>
  );
};
